name: 'spark_utils'
version: '0.1.0'
config-version: 2

require-dbt-version: ">=0.18.0"

target-path: "target"
clean-targets: ["target", "dbt_modules"]
macro-paths: ["macros"]
log-path: "logs"

vars:
  utils_namespaces: spark_utils
